{
  "name": "dexcandles-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ dexcandles-subgraph",
    "create-local": "graph create --node http://localhost:8020/ dexcandles-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ dexcandles-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dexcandles-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.32.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
